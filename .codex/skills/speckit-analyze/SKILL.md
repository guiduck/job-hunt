---
name: "speckit-analyze"
description: "Perform a non-destructive cross-artifact consistency and quality analysis across spec.md, plan.md, and tasks.md after task generation."
compatibility: "Requires spec-kit project structure with .specify/ directory"
metadata:
  author: "github-spec-kit"
---

# Speckit Analyze

This is the Codex mirror for `.cursor/skills/speckit-analyze/SKILL.md`.

Follow the Cursor skill as the canonical workflow, with this Codex path mapping:

- When the Cursor skill references `.cursor/skills/<name>/SKILL.md`, use `.codex/skills/<name>/SKILL.md`.
- Keep shared Spec Kit paths unchanged: `.specify/`, feature `spec.md`, `plan.md`, `tasks.md`, and `specs/`.
- Keep this workflow non-destructive unless the user explicitly asks for edits.

Cursor support remains available through the original `.cursor/skills/speckit-analyze/SKILL.md`.
