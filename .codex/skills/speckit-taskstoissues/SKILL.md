---
name: "speckit-taskstoissues"
description: "Convert existing tasks into actionable, dependency-ordered GitHub issues for the feature based on available design artifacts."
compatibility: "Requires spec-kit project structure with .specify/ directory"
metadata:
  author: "github-spec-kit"
---

# Speckit Tasks To Issues

This is the Codex mirror for `.cursor/skills/speckit-taskstoissues/SKILL.md`.

Follow the Cursor skill as the canonical workflow, with this Codex path mapping:

- When the Cursor skill references `.cursor/skills/<name>/SKILL.md`, use `.codex/skills/<name>/SKILL.md`.
- Keep shared Spec Kit paths unchanged: `.specify/`, feature `tasks.md`, `plan.md`, `spec.md`, and `specs/`.
- Do not create GitHub issues unless the user explicitly requests that side effect.

Cursor support remains available through the original `.cursor/skills/speckit-taskstoissues/SKILL.md`.
